<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%=link_to "LibHome", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <%= link_to "Home", root_path, class: "nav-link active"%>
      </div>

      <div class="navbar-nav ms-auto mb-2">
          <% if Current.user%>
              <span class="text-primary d-flex align-items-center border rounded fw-bold me-2">
                <%= link_to "See Books", see_books_path, class:"nav-link"%>
              </span>
              <span class="text-primary d-flex align-items-center border rounded fw-bold ps-2 pe-2">
                <%= Current.user.email%>
              </span>
              <%= button_to "Logout", logout_path, method: :delete, class:"btn btn-outline-secondary ms-2"%>
          <% else %>
            <%= link_to "Sign Up", sign_up_url, class: "nav-link" %>
            <%= link_to "Login", sign_in_path, class: "nav-link" %>
          <%end%>
      </div>
    </div>
  </div>
</nav>